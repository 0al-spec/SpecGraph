# Source Draft: Agent Passport Reference Declaration

Operator request:

> Отличный план и скорость продвижения, делаем 0070!

Plan captured by the proposal:

- declare Agent Passport refs for required graph-facing surfaces that were still
  missing refs after the 0067 report-only derived surfaces;
- target `specgraph.supervisor.executor_adapter`,
  `specspace.operator_assistant`, and `product_workspace.implementation_agent`;
- remove `missing_passport` gaps from the derived indexes without claiming
  schema/signature/lifecycle verification;
- keep runtime enforcement out of scope and preserve
  `runtime_enforcement_unknown` until a policy runtime observes enforcement;
- leave SpecSpace and Platform unchanged.

