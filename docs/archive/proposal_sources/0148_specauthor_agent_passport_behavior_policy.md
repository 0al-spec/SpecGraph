# 0148 SpecAuthorAgent Agent Passport Behavior Policy

SpecAuthorAgent gets a local report-only Agent Passport declaration with an
experimental `x-behaviorPolicies` extension. The extension references the
SpecGraph prompt-side authoring, generated artifact, invocation artifact, and
ontology write-gate contracts without upgrading those behavioral declarations
to runtime security enforcement.
