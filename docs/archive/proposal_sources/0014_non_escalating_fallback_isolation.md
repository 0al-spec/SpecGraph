# Non-Escalating Fallback Isolation Source Draft

## Source Class

Working draft.

## Historical Concern

SpecGraph needed degraded supervisor fallback execution to preserve or narrow
authority rather than silently widening sandbox or write privileges when the
primary worktree path failed.

## Promotion Note

This archived source preserves the draft concern that was normalized into
`docs/proposals/0014_non_escalating_fallback_isolation.md`.
