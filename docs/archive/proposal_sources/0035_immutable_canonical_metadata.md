# Immutable Canonical Metadata Source Draft

## Source Class

Working draft.

## Historical Concern

SpecGraph needed ordinary gate approval to preserve immutable canonical
metadata such as `id` and `created_at` so semantic candidate acceptance could
not accidentally rewrite identity or creation-time facts.

## Promotion Note

This archived source preserves the draft concern that was normalized into
`docs/proposals/0035_immutable_canonical_metadata_guardrails.md`.
