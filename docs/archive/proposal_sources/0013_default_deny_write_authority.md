# Default-Deny Write Authority Source Draft

## Source Class

Working draft.

## Historical Concern

SpecGraph needed supervisor write authority to be default-deny so missing or
empty `allowed_paths` would not imply unrestricted canonical writeback or
accidental structural mutation.

## Promotion Note

This archived source preserves the draft concern that was normalized into
`docs/proposals/0013_default_deny_write_authority.md`.
