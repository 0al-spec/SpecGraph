# Default Supervisor Prompt Overlay

Use the normal SpecGraph supervisor behavior.

This overlay is intentionally minimal. It exists as a reviewable baseline for
project-specific prompt profiles and must not change authority, validation,
allowed paths, protocol markers, evidence semantics, or review gates.
