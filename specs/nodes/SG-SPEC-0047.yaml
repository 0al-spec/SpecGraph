id: SG-SPEC-0047
title: SpecGraph - Historical Gateway Segment Slice
kind: spec
created_at: '2026-04-14T20:59:56Z'
updated_at: '2026-04-17T21:18:34Z'
status: reviewed
maturity: 0.4
presence:
  state: historical
  lineage_basis: superseded_lineage
depends_on:
- SG-SPEC-0045
refines:
- SG-SPEC-0045
superseded_by:
- SG-SPEC-0045
inputs:
- specs/nodes/SG-SPEC-0045.yaml
outputs:
- specs/nodes/SG-SPEC-0047.yaml
allowed_paths:
- specs/nodes/SG-SPEC-0047.yaml
acceptance:
- Records SG-SPEC-0047 as a superseded decomposition artifact from an earlier SG-SPEC-0045 split attempt.
- Preserves inspectable lineage to SG-SPEC-0045 without claiming a distinct active node_role or composed_function.
- Must not be treated as an active refinement child while SG-SPEC-0045 remains the superseding readable
  contract.
acceptance_evidence:
- criterion: Records SG-SPEC-0047 as a superseded decomposition artifact from an earlier SG-SPEC-0045
    split attempt.
  evidence: top-level superseded_by marks SG-SPEC-0045 as the superseding contract, and specification.historical_status
    states that SG-SPEC-0047 remains inspectable only as a prior split attempt.
- criterion: Preserves inspectable lineage to SG-SPEC-0045 without claiming a distinct active node_role
    or composed_function.
  evidence: refines keeps SG-SPEC-0045 as the lineage anchor, while specification.boundaries says SG-SPEC-0047
    no longer defines an active runtime-observable role distinct from SG-SPEC-0045.
- criterion: Must not be treated as an active refinement child while SG-SPEC-0045 remains the superseding
    readable contract.
  evidence: specification.inactive_rules require graph-health inspection and future subtree work to treat
    SG-SPEC-0047 as inactive while superseded_by points to SG-SPEC-0045.
prompt: Preserve SG-SPEC-0047 only as inspectable lineage for a superseded split attempt beneath SG-SPEC-0045.
  Do not present it as an active subtree role.
specification:
  objective: Preserve SG-SPEC-0047 as historical lineage for a superseded split attempt beneath SG-SPEC-0045
    without treating it as an active role-bearing child.
  scope:
    in:
    - Inspectable lineage for one superseded decomposition attempt beneath SG-SPEC-0045.
    - Explicit superseded_by relation to the active readable contract in SG-SPEC-0045.
    - Inactive-child rules for active subtree reads and graph-health inspection.
    out:
    - Any distinct active runtime-observable invariant beyond SG-SPEC-0045.
    - New decomposition authority, child materialization, or queue-control semantics.
    - Concrete proposal/split mechanics, ranking policy, or runtime orchestration.
  historical_status:
  - SG-SPEC-0047 records a prior gateway segment slice that has been superseded by SG-SPEC-0045.
  - SG-SPEC-0047 remains inspectable lineage only and does not define the active subtree role.
  lineage_contract:
  - SG-SPEC-0045 supersedes SG-SPEC-0047.
  - SG-SPEC-0047 refines SG-SPEC-0045 only as historical lineage, not as an active child for continued
    splitting.
  inactive_rules:
  - Graph-health inspection must exclude SG-SPEC-0047 from the active SG-SPEC-0045 subtree while superseded_by
    points to SG-SPEC-0045.
  - Future refinement should target SG-SPEC-0045 rather than reopening SG-SPEC-0047 unless a later reviewed
    supersession changes lineage again.
  boundaries:
  - SG-SPEC-0047 no longer defines an active node_role, composed_function, or downstream contract.
  - This node exists only to preserve inspectable lineage for the superseded split attempt.
gate_state: none
proposed_status: null
proposed_maturity: null
required_human_action: '-'
last_worktree_path: ''
last_branch: ''
last_gate_decision: approve
last_gate_note: superseded by SG-SPEC-0045 readable merge
last_gate_at: '2026-04-17T00:00:00+00:00'
