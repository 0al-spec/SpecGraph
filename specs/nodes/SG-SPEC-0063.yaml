id: SG-SPEC-0063
title: SpecGraph - Realization and Observation Semantics Cluster
kind: spec
created_at: '2026-05-14T22:30:00Z'
updated_at: '2026-05-20T06:55:41Z'
status: linked
maturity: 0.88
depends_on:
- SG-SPEC-0017
- SG-SPEC-0018
- SG-SPEC-0020
- SG-SPEC-0021
relates_to:
- SG-SPEC-0003
- SG-SPEC-0007
refines:
- SG-SPEC-0001
inputs:
- specs/nodes/SG-SPEC-0001.yaml
- specs/nodes/SG-SPEC-0017.yaml
- specs/nodes/SG-SPEC-0018.yaml
- specs/nodes/SG-SPEC-0020.yaml
- specs/nodes/SG-SPEC-0021.yaml
- docs/proposals/0022_refinement_fan_out_pressure.md
outputs:
- specs/nodes/SG-SPEC-0063.yaml
allowed_paths:
- specs/nodes/SG-SPEC-0063.yaml
acceptance:
- Defines SG-SPEC-0063 as the intermediate semantic cluster parent under SG-SPEC-0001 for realization,
  observation, adoption, executable traceability, and derived metric-family semantics.
- Groups runtime observability entities, adoption-layer entities, executable-layer traceability, and derived
  metric family boundaries without redefining the bounded contracts owned by those children.
- Defines root dependency routing so SG-SPEC-0001 can depend on one realization-and-observation aggregate
  while concrete child specs keep their existing semantic ownership and dependencies.
- Defines cross-member handoff semantics so realization, observation, adoption, executable, and metric-family
  concerns route to the owning member spec when evidence or gaps span more than one member boundary.
- Keeps implementation tooling, telemetry collection, dashboards, metric formulas, adoption operations,
  and repository layout out of scope for this cluster.
acceptance_evidence:
- criterion: Defines SG-SPEC-0063 as the intermediate semantic cluster parent under SG-SPEC-0001 for realization,
    observation, adoption, executable traceability, and derived metric-family semantics.
  evidence: specification.objective, specification.scope.in, and specification.cluster_contract.parent_boundary
    define SG-SPEC-0063 as the delegated realization-and-observation cluster beneath SG-SPEC-0001, while
    inputs include the fan-out pressure proposal that motivates intermediate clustering.
- criterion: Groups runtime observability entities, adoption-layer entities, executable-layer traceability,
    and derived metric family boundaries without redefining the bounded contracts owned by those children.
  evidence: specification.member_contracts enumerates SG-SPEC-0017, SG-SPEC-0018, SG-SPEC-0020, and SG-SPEC-0021
    with explicit member roles and ownership boundaries.
- criterion: Defines root dependency routing so SG-SPEC-0001 can depend on one realization-and-observation
    aggregate while concrete child specs keep their existing semantic ownership and dependencies.
  evidence: specification.cluster_contract.dependency_policy and specification.cluster_contract.refinement_routing_policy
    state that SG-SPEC-0001 should depend on SG-SPEC-0063 for the aggregate, while members retain their
    bounded contracts and may be reparented under the cluster without changing stable spec identity.
- criterion: Defines cross-member handoff semantics so realization, observation, adoption, executable,
    and metric-family concerns route to the owning member spec when evidence or gaps span more than one
    member boundary.
  evidence: specification.cross_member_handoff_policy defines how multi-member realization and observation
    concerns identify a primary owning member, reference supporting members, preserve canonical-versus-derived
    boundaries, and route member-specific gaps back to the concrete owning spec.
- criterion: Keeps implementation tooling, telemetry collection, dashboards, metric formulas, adoption
    operations, and repository layout out of scope for this cluster.
  evidence: specification.scope.out excludes implementation tooling, telemetry collection, dashboards,
    metric formulas, adoption operations, and repository layout; specification.non_goal_policy forbids
    runtime realization claims and wrapper-only clustering.
prompt: Introduce the realization and observation semantics cluster beneath SG-SPEC-0001 so root fan-out
  pressure can be reduced through a meaningful intermediate parent for runtime, adoption, executable,
  and metric-family semantics.
specification:
  objective: Define the intermediate semantic cluster for the graph-to-realization boundary so SG-SPEC-0001
    can delegate runtime observability, adoption semantics, executable traceability, and derived metric
    family concerns through one legible aggregate boundary while each concrete child keeps its stable
    bounded contract.
  scope:
    in:
    - Intermediate cluster role for realization and observation semantics beneath SG-SPEC-0001
    - Grouping boundary for runtime observability, adoption-layer entities, executable-layer traceability,
      and derived metric-family semantics
    - Dependency routing from SG-SPEC-0001 through the cluster to concrete semantic children
    - Reparenting boundary for reducing root refinement fan-out without changing stable child IDs
    - Canonical-versus-derived protection for observation and metric-family semantics
    - Cross-member handoff semantics for realization and observation concerns spanning more than one member
      boundary
    out:
    - Runtime instrumentation, telemetry collection, storage schemas, or observability backends
    - Metric formulas, threshold policies, dashboards, ranking, or scheduler behavior
    - UX taxonomy, analytics event schemas, campaign operations, or adoption execution
    - Repository layout, CI workflow, build tooling, deployment mechanics, or code-generation strategy
    - Viewer UI, graph layout, badges, alerts, or interaction design
    - Runtime implementation or dedicated executor ownership for these semantics
  terminology:
    realization_observation_cluster: The intermediate parent under SG-SPEC-0001 that groups semantics
      connecting canonical graph subjects to execution, observation, adoption, and metric interpretation.
    realization_boundary: The semantic frontier where canonical specs become executable anchors, tests,
      releases, or implementation-facing traceability subjects without making operational artifacts authoritative.
    observation_boundary: The semantic frontier where runtime evidence, adoption evidence, and derived
      metric families inform graph understanding without becoming canonical truth by default.
    cluster_member_contract: A stable child spec whose bounded semantics are grouped by SG-SPEC-0063 without
      losing its own identity, acceptance criteria, or dependency relationships.
    cross_member_handoff: A realization or observation concern that touches more than one cluster member
      but still requires one primary owning member for acceptance, evidence, and backlog routing.
  cluster_contract:
    parent_boundary:
    - SG-SPEC-0001 remains the seed ontology and owns root layer, node kind, edge kind, lifecycle, and
      invariant semantics.
    - SG-SPEC-0063 owns the aggregate boundary for the graph-to-realization and graph-to-observation semantic
      region.
    - The cluster exists only because runtime observability, adoption semantics, executable traceability,
      and metric families form a real downstream observation-and-realization aggregate.
    dependency_policy:
    - SG-SPEC-0001 may depend on SG-SPEC-0063 as the aggregate blocking descendant for realization and
      observation semantics.
    - SG-SPEC-0063 depends on concrete member specs that define the detailed semantics required by the
      aggregate.
    - Member specs keep their stable IDs and may retain their existing own dependencies; the cluster does
      not rewrite those contracts.
    refinement_routing_policy:
    - Existing SG-SPEC-0001 children whose primary role is runtime observability, adoption, executable
      traceability, or derived metric-family semantics may be reparented to SG-SPEC-0063.
    - Reparenting a member under SG-SPEC-0063 must preserve the member's stable identity, acceptance,
      provenance, and own dependency semantics.
    - SG-SPEC-0001 should reference the cluster for the aggregate and avoid repeating a flat list of every
      downstream realization or observation member when the cluster owns that grouping.
  member_contracts:
  - spec_id: SG-SPEC-0017
    member_role: Runtime observability entity boundary.
    ownership_boundary: Defines ObservabilityContract, Campaign, and Attestation semantics plus evidence
      boundaries without defining telemetry collection or instrumentation.
  - spec_id: SG-SPEC-0018
    member_role: Adoption-layer entity boundary.
    ownership_boundary: Defines DiscoverySurface and AdoptionCriterion semantics without defining UX taxonomy,
      analytics formulas, or campaign operations.
  - spec_id: SG-SPEC-0020
    member_role: Executable-layer traceability boundary.
    ownership_boundary: Defines code_surface, test, release, and executable trace links without prescribing
      repository layout, CI, build, deployment, or language choices.
  - spec_id: SG-SPEC-0021
    member_role: Derived metric family boundary.
    ownership_boundary: Defines metric-family semantic handles and canonical-versus-derived separation
      without defining formulas, thresholds, dashboards, storage, or scheduler behavior.
  cross_member_handoff_policy:
  - A concern spanning runtime observability, adoption, executable traceability, or metric-family semantics
    must name one primary member spec as the owner of the acceptance claim or unresolved gap.
  - Supporting members may be referenced as inputs or evidence context, but those references do not transfer
    ownership of their bounded semantics to SG-SPEC-0063.
  - Executable traces that produce runtime evidence remain executable-layer traceability when the claim
    is about code_surface, test, release, or executable links, and become runtime observability evidence
    only when interpreted through ObservabilityContract, Campaign, or Attestation semantics.
  - Adoption evidence that is summarized by a metric family remains adoption-layer evidence at the source
    and derived metric-family semantics at the summary boundary; the summary must not become canonical
    adoption truth by default.
  - Backlog, review, or evidence gaps that are member-specific must route to the concrete owning member
    rather than being closed at the cluster aggregate boundary.
  non_goal_policy:
  - SG-SPEC-0063 must not redefine member formulas, runtime entity schemas, telemetry pipelines, or viewer
    affordances.
  - SG-SPEC-0063 must not claim runtime realization merely because its members name executable or observation
    surfaces.
  - SG-SPEC-0063 must not hide unresolved member gaps; backlog and evidence projections should continue
    to point to the concrete member when the gap is member-specific.
gate_state: none
proposed_status: null
proposed_maturity: null
required_human_action: '-'
last_worktree_path: ''
last_branch: ''
last_gate_decision: approve
last_gate_note: ''
last_gate_at: '2026-05-20T06:55:41.934026+00:00'
last_gate_run_id: 20260519T214802Z-SG-SPEC-0063-80e51a2c
last_prompt_overlay_provenance:
  enabled: true
  source_kind: profile
  prompt_profile_id: default
  prompt_extension_path: tools/supervisor_prompts/default.md
  prompt_extension_sha256: 12e2eb9fe436d018cec8e97924fa146b7d4fc8d64ff173a49db80a7a6f6acab5
  prompt_overlay_authority: project
  core_prompt_overridden: false
  policy_reference:
    artifact_path: tools/supervisor_prompt_policy.json
    artifact_sha256: 88d2c46d25b2ed732f7cf1694d7a2bcf4455962d1b0dd290cb9993fbe35a728d
    version: 1
  non_overridable_invariants:
  - protocol_markers_required
  - allowed_paths_required
  - review_gates_required
  - evidence_semantics_required
  - artifact_contract_validation_required
