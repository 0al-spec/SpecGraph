id: SG-SPEC-0032
title: SpecGraph - Proposal/Split Mechanics Subcluster
kind: spec
created_at: '2026-04-14T05:05:41Z'
updated_at: '2026-04-29T22:36:53Z'
status: linked
maturity: 0.69
depends_on:
- SG-SPEC-0043
refines:
- SG-SPEC-0027
inputs:
- specs/nodes/SG-SPEC-0027.yaml
- specs/nodes/SG-SPEC-0029.yaml
- tasks.md
- specs/nodes/SG-SPEC-0043.yaml
outputs:
- specs/nodes/SG-SPEC-0032.yaml
allowed_paths:
- specs/nodes/SG-SPEC-0032.yaml
acceptance:
- Defines proposal/split mechanics readiness as one coherent capability region inside reflective execution.
- Explains how reflective execution can ask whether proposal/split mechanics are ready without reading
  split payload schemas or proposal-lane runtime details.
- Keeps continuation selection insulated from proposal/split mechanics detail while preserving reviewable
  readiness for that mechanics region.
- 'Applies the topology-prose rule for this branch: graph edges remain metadata, while prose explains
  runtime readiness and review meaning.'
- Keeps this node implementation-agnostic to queue ranking, continuation selection, viewer layout, and
  runtime orchestration.
acceptance_evidence:
- criterion: Defines proposal/split mechanics readiness as one coherent capability region inside reflective
    execution.
  evidence: specification.readiness_region and specification.execution_meaning define proposal/split readiness
    as the runtime-facing capability that this node contributes.
- criterion: Explains how reflective execution can ask whether proposal/split mechanics are ready without
    reading split payload schemas or proposal-lane runtime details.
  evidence: specification.readiness_query describes the coarse readiness question, while specification.scope.out
    excludes split payload schemas and proposal-lane runtime detail.
- criterion: Keeps continuation selection insulated from proposal/split mechanics detail while preserving
    reviewable readiness for that mechanics region.
  evidence: specification.continuation_boundary and specification.parent_contract separate continuation
    selection from proposal/split readiness detail.
- criterion: 'Applies the topology-prose rule for this branch: graph edges remain metadata, while prose
    explains runtime readiness and review meaning.'
  evidence: specification.topology_prose_policy says direct child, edge, and delegation facts are represented
    by canonical metadata and should not become the main prose of this node.
- criterion: Keeps this node implementation-agnostic to queue ranking, continuation selection, viewer
    layout, and runtime orchestration.
  evidence: specification.scope.out excludes queue ranking, continuation-selection policy, viewer layout,
    and runtime orchestration.
prompt: Keep this node as the proposal/split mechanics readiness region inside reflective execution. It
  should explain what readiness means to the runtime and reviewers without narrating graph topology that
  already exists in metadata.
specification:
  objective: Define proposal/split mechanics readiness as one coherent capability region inside reflective
    execution, so higher-level continuation logic can reason about availability without inspecting concrete
    split payloads, queue formulas, or proposal-lane runtime details.
  scope:
    in:
    - Proposal/split mechanics readiness as a runtime capability region
    - Coarse readiness query exposed to reflective execution
    - Review boundary between execution readiness and concrete split lifecycle mechanics
    - Continuation-selection insulation from proposal/split mechanics detail
    - Topology-prose discipline for this branch
    out:
    - Field-level split artifact schemas owned by downstream mechanics contracts
    - Continuation-selection policy
    - Queue ranking formulas, viewer layout, and runtime orchestration
  readiness_region:
  - Proposal/split mechanics readiness is the coarse runtime condition that proposal/split behavior can
    be considered available for reflective execution.
  - This node keeps that readiness concept readable as a capability region rather than as a chain of graph
    edges.
  execution_meaning:
  - Reflective execution can treat proposal/split readiness as satisfied only when the downstream mechanics
    boundary can answer the availability question without exposing split payload internals.
  - This node does not decide which continuation candidate wins and does not rank proposal-lane work.
  readiness_query:
  - Is proposal/split mechanics available as a reviewable capability for the current reflective execution
    context?
  - If the answer cannot be determined without split payload or queue internals, this node remains too
    leaky and should be refined around readiness meaning rather than topology.
  continuation_boundary:
  - Continuation selection may consume the result of proposal/split readiness indirectly through reflective
    execution readiness.
  - Continuation selection must not inspect concrete proposal/split lifecycle fields to choose the next
    target.
  topology_prose_policy:
  - Direct edge, child, and delegation facts for this branch are represented by canonical graph metadata.
  - This node's prose should explain readiness, review meaning, and insulation boundaries rather than
    restating which spec ID routes to which other spec ID.
  cluster_boundaries:
  - This node does not redefine downstream mechanics semantics.
  - This node does not define continuation-selection rules, viewer behavior, queue ranking formulas, or
    runtime orchestration.
  role_boundary:
  - This node owns only the coarse proposal/split readiness region exposed to reflective execution.
  - This node does not own concrete mechanics semantics, split payload schemas, continuation-selection
    policy, viewer behavior, queue ranking, or runtime orchestration.
  - Graph placement is represented by metadata edges; prose should describe readiness semantics rather
    than restating provider routes.
  parent_contract:
  - The reflective execution-readiness layer consumes this node as the proposal/split readiness region.
  - This node keeps continuation selection separate from concrete proposal/split mechanics detail.
  - This node should be rewritten or merged rather than split again if the proposed child would only verbalize
    graph topology.
gate_state: none
required_human_action: '-'
proposed_status: null
proposed_maturity: null
last_outcome: split_required
last_blocker: ''
last_run_id: 20260414T192924Z-SG-SPEC-0032
last_exit_code: 0
last_changed_files:
- specs/nodes/SG-SPEC-0032.yaml
- specs/nodes/SG-SPEC-0043.yaml
last_run_at: '2026-04-14T19:29:25.138520+00:00'
last_worktree_path: /Users/egor/Development/GitHub/0AL/SpecGraph/.worktrees/sg-spec-0032-20260414T192850Z
last_branch: codex/sg-spec-0032/20260414T192850Z
last_validator_results:
  outputs: true
  allowed_paths: true
  reconciliation: true
  atomicity: true
  transition: true
  executor_environment: true
  refinement_acceptance: true
last_refinement_acceptance:
  decision: review_required
  change_class: graph_refactor
  checks:
    content_changed: true
    hard_validation: true
    single_spec_scope: false
    constitutional_diff: false
    graph_refactor_diff: true
    atomicity_clear: true
    within_mutation_budget: true
  diff_paths:
  - acceptance[1]
  - acceptance_evidence[1].criterion
  - acceptance_evidence[1].evidence
  - acceptance_evidence[2].evidence
  - depends_on[0]
  - inputs[3]
  - specification.cluster_boundaries[0]
  - specification.cluster_members.proposal_gateway[0]
  - specification.cluster_members.proposal_gateway[1]
  - specification.cluster_role[1]
  - specification.cluster_topology[1]
  - specification.cluster_topology[2]
  - specification.cluster_topology[3]
  - specification.objective
  - specification.parent_contract[1]
  - specification.parent_contract[2]
  - specification.scope.in[1]
  - specification.scope.in[3]
  mutation_classes:
  - policy_text
  mutation_budget: []
  budget_exceeded_classes: []
  changed_spec_files:
  - specs/nodes/SG-SPEC-0032.yaml
  - specs/nodes/SG-SPEC-0043.yaml
  review_reasons:
  - run changed additional spec files beyond the selected source node
  - run changed graph structure or spec contract paths
  errors: []
  warnings: []
last_reconciliation:
  worktree_spec_count: 43
  changed_spec_ids:
  - SG-SPEC-0032
  - SG-SPEC-0043
  semantic_dependencies_resolved: true
  work_dependencies_ready: false
  cycles: []
last_requested_child_materialization: true
last_materialized_child_paths:
- specs/nodes/SG-SPEC-0043.yaml
last_gate_decision: approve
last_gate_note: topology-prose cleanup rewrote SG-SPEC-0032 as readiness-region prose
last_gate_at: '2026-04-25T07:10:00+00:00'
