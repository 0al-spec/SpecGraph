id: SG-SPEC-0028
title: SpecGraph - Proposal/Split Mechanics Coordination
kind: spec
created_at: '2026-04-13T07:55:45Z'
updated_at: '2026-04-27T21:14:36Z'
status: linked
maturity: 0.89
depends_on:
- SG-SPEC-0006
- SG-SPEC-0025
refines:
- SG-SPEC-0027
inputs:
- specs/nodes/SG-SPEC-0027.yaml
- specs/nodes/SG-SPEC-0006.yaml
- specs/nodes/SG-SPEC-0025.yaml
- tasks.md
outputs:
- specs/nodes/SG-SPEC-0028.yaml
allowed_paths:
- specs/nodes/SG-SPEC-0028.yaml
acceptance:
- Defines the proposal/split mechanics coordination surface consumed by reflective execution before concrete
  proposal-lane or split payload details are inspected.
- Keeps proposal-lane execution and split mechanics jointly reviewable as separate required capabilities
  without turning their graph placement into the main contract.
- Preserves concrete ownership boundaries where SG-SPEC-0006 owns proposal-lane execution, SG-SPEC-0025
  owns split mechanics and payload semantics, SG-SPEC-0004 remains the split lifecycle anchor, and SG-SPEC-0030
  owns parent-facing prerequisite semantics.
- Separates proposal/split mechanics readiness from retrospective direct-update semantics owned by SG-SPEC-0005.
- Remains implementation-agnostic about proposal storage layout, queue ranking formulas, and runtime orchestration.
acceptance_evidence:
- criterion: Defines the proposal/split mechanics coordination surface consumed by reflective execution
    before concrete proposal-lane or split payload details are inspected.
  evidence: specification.coordination_surface and specification.readiness_question define the runtime-facing
    coordination role without exposing descendant payload or queue fields.
- criterion: Keeps proposal-lane execution and split mechanics jointly reviewable as separate required
    capabilities without turning their graph placement into the main contract.
  evidence: specification.joint_reviewability_conditions requires separate review outcomes for proposal-lane
    execution and split mechanics, while specification.topology_prose_policy says their graph placement
    remains metadata.
- criterion: Preserves concrete ownership boundaries where SG-SPEC-0006 owns proposal-lane execution,
    SG-SPEC-0025 owns split mechanics and payload semantics, SG-SPEC-0004 remains the split lifecycle
    anchor, and SG-SPEC-0030 owns parent-facing prerequisite semantics.
  evidence: specification.ownership_boundaries names each concrete owner and limits SG-SPEC-0028 to coordination
    semantics.
- criterion: Separates proposal/split mechanics readiness from retrospective direct-update semantics owned
    by SG-SPEC-0005.
  evidence: specification.integration_invariants and specification.boundaries state that SG-SPEC-0005
    direct-update semantics are not implied by this coordination surface.
- criterion: Remains implementation-agnostic about proposal storage layout, queue ranking formulas, and
    runtime orchestration.
  evidence: specification.scope.out excludes proposal storage layout, queue ranking formulas, and runtime
    orchestration details. SG-SPEC-0028 governs only coordination and ownership boundaries.
prompt: Keep SG-SPEC-0028 as the proposal/split mechanics coordination surface for reflective execution.
  It should explain how proposal-lane execution and split mechanics stay jointly reviewable without narrating
  child placement, edge shape, route narration, or queue/runtime implementation detail.
specification:
  objective: Define the proposal/split mechanics coordination surface that lets reflective execution reason
    about proposal-lane execution and split mechanics as one coherent capability set without merging their
    concrete contracts or narrating graph topology.
  scope:
    in:
    - Proposal/split mechanics coordination for reflective execution.
    - Joint reviewability of proposal-lane execution and split mechanics.
    - Ownership boundaries for SG-SPEC-0006, SG-SPEC-0025, SG-SPEC-0004, and SG-SPEC-0030.
    - Separation from retrospective direct-update semantics owned by SG-SPEC-0005.
    - Topology-prose discipline for this mechanics region.
    out:
    - Concrete mechanics semantics already owned by descendant specs.
    - Retrospective direct-update boundary semantics owned by SG-SPEC-0005.
    - Viewer layout, queue ranking formulas, proposal storage layout, and runtime orchestration.
    - Prose whose main purpose is to restate edge order, child placement, or route narration.
  coordination_surface:
  - Reflective execution may treat proposal/split mechanics as a coherent capability set only when proposal-lane
    execution and split mechanics remain separately reviewable.
  - This node answers whether the mechanics region is coherent enough to consume as a readiness input;
    it does not expose split payload fields, queue formulas, or proposal-lane runtime details.
  readiness_question:
  - Are proposal-lane execution and split mechanics both available as reviewable capabilities for reflective
    execution without collapsing their ownership boundaries?
  joint_reviewability_conditions:
  - Proposal/split mechanics are jointly reviewable only when proposal-lane execution and split mechanics
    can each receive an independent review outcome before reflective execution treats the capability set
    as ready.
  - A review finding against proposal-lane execution must name the proposal-lane capability as unresolved
    without implying that split mechanics failed.
  - A review finding against split mechanics must name the split mechanics capability as unresolved without
    implying that proposal-lane execution failed.
  - A readiness conclusion must explain the capability-level coordination result without narrating child
    placement, edge shape, route order, queue ordering, or runtime scheduling.
  required_capabilities:
    proposal_lane_execution:
    - SG-SPEC-0006 owns proposal-lane execution semantics, tracked proposal identity, and execution-lane
      behavior.
    split_mechanics:
    - SG-SPEC-0025 owns split mechanics, split payload semantics, and split queue mechanics.
    split_lifecycle_anchor:
    - SG-SPEC-0004 remains the deterministic split proposal lifecycle anchor cited through split mechanics.
    parent_prerequisite_contract:
    - SG-SPEC-0030 owns parent-facing prerequisite semantics for consuming this coordination surface.
  ownership_boundaries:
  - SG-SPEC-0028 may cite SG-SPEC-0006 and SG-SPEC-0025 as required mechanics capabilities, but it must
    not restate their concrete payload, queue, or execution contracts.
  - SG-SPEC-0028 may cite SG-SPEC-0004 as the split lifecycle anchor, but it must not redefine lifecycle
    transitions.
  - SG-SPEC-0030 owns the consumer-facing prerequisite rule; SG-SPEC-0028 owns only the coordinated mechanics
    meaning consumed by that rule.
  integration_invariants:
  - Proposal-lane execution must not be treated as a substitute for split mechanics.
  - Split mechanics must not be treated as a substitute for proposal-lane execution.
  - Proposal/split readiness must not imply retrospective direct-update authority from SG-SPEC-0005.
  topology_prose_policy:
  - Direct graph relations around SG-SPEC-0028 are represented by canonical fields and derived graph views.
  - SG-SPEC-0028 prose should explain coordination, ownership, and readiness meaning rather than child
    placement, edge order, or route narration.
  boundaries:
  - SG-SPEC-0028 does not collapse descendant node identities or their bounded concerns.
  - Cross-capability consistency is expressed here only at the proposal/split mechanics coordination level.
  - If a future refinement would only verbalize a direct edge, route, or delegation fact, prefer rewrite,
    merge, regrouping, or proposal pressure instead of a new canonical child.
  parent_contract:
  - SG-SPEC-0027 consumes SG-SPEC-0028 as the proposal/split mechanics coordination surface for reflective
    execution readiness.
  - Parent-facing prerequisite semantics are owned by SG-SPEC-0030 and should not be duplicated here as
    edge-order prose.
  - SG-SPEC-0027 must not read SG-SPEC-0028 as a proxy for SG-SPEC-0005; direct-update semantics remain
    a separately consumed concern.
gate_state: none
proposed_status: null
proposed_maturity: null
required_human_action: '-'
last_worktree_path: ''
last_branch: ''
last_gate_decision: approve
last_gate_note: ''
last_gate_at: '2026-04-27T21:14:36.668095+00:00'
