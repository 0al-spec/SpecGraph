id: SG-SPEC-0026
title: SpecGraph - Reflective Mechanics Integration and Continuation Selection
kind: spec
created_at: '2026-04-13T07:46:44Z'
updated_at: '2026-04-25T07:10:00Z'
status: linked
maturity: 1.0
depends_on:
- SG-SPEC-0027
- SG-SPEC-0033
refines:
- SG-SPEC-0003
inputs:
- specs/nodes/SG-SPEC-0003.yaml
- specs/nodes/SG-SPEC-0004.yaml
- specs/nodes/SG-SPEC-0005.yaml
- specs/nodes/SG-SPEC-0006.yaml
- specs/nodes/SG-SPEC-0025.yaml
- specs/nodes/SG-SPEC-0027.yaml
- specs/nodes/SG-SPEC-0033.yaml
- tasks.md
outputs:
- specs/nodes/SG-SPEC-0026.yaml
allowed_paths:
- specs/nodes/SG-SPEC-0026.yaml
acceptance:
- Defines the continuation decision boundary that decides whether a completed reflective run may hand
  off to another bounded target.
- 'Separates candidate selection from execution readiness: candidate derivation and tie-break stay in
  SG-SPEC-0033, while execution readiness is consumed from SG-SPEC-0027.'
- Defines suppression and handoff behavior for continuation attempts without mutating canonical backlog
  state directly.
- Applies the topology-prose rule for this branch so integration prose explains continuation behavior
  rather than child routing.
- Avoids scoring formulas, queue-priority engines, viewer/UX contracts, and concrete mechanics payloads.
acceptance_evidence:
- criterion: Defines the continuation decision boundary that decides whether a completed reflective run
    may hand off to another bounded target.
  evidence: specification.continuation_boundary and specification.decision_contract define the decision
    point and the possible handoff/suppression outcomes.
- criterion: 'Separates candidate selection from execution readiness: candidate derivation and tie-break
    stay in SG-SPEC-0033, while execution readiness is consumed from SG-SPEC-0027.'
  evidence: specification.decision_inputs and specification.boundaries separate candidate selection from
    execution readiness without embedding either child topology in the prose as the main concern.
- criterion: Defines suppression and handoff behavior for continuation attempts without mutating canonical
    backlog state directly.
  evidence: specification.continuation_selection.suppression and specification.continuation_selection.handoff
    define derived continuation records and non-canonical persistence expectations.
- criterion: Applies the topology-prose rule for this branch so integration prose explains continuation
    behavior rather than child routing.
  evidence: specification.topology_prose_policy says graph relations are metadata and SG-SPEC-0026 prose
    should explain the continuation decision semantics.
- criterion: Avoids scoring formulas, queue-priority engines, viewer/UX contracts, and concrete mechanics
    payloads.
  evidence: specification.scope.out excludes scoring formulas, queue-priority engines, viewer/UX contracts,
    concrete mechanics payloads, and concrete artifact mutation contracts.
prompt: Keep SG-SPEC-0026 as the continuation decision boundary for reflective execution. It should explain
  when a completed run may hand off to another bounded target, how candidate selection and execution readiness
  are consumed, and when continuation is suppressed without turning graph topology into prose.
specification:
  objective: Define the continuation decision boundary that consumes candidate-selection results and reflective
    execution readiness to decide whether continuation is handed off, suppressed, or kept as a derived
    artifact.
  scope:
    in:
    - Continuation decision boundary after a completed reflective run
    - Continuation preconditions, suppression triggers, and canonical handoff behavior
    - Consumption of deterministic continuation candidate selection from SG-SPEC-0033
    - Consumption of reflective execution readiness from SG-SPEC-0027
    - Non-canonical persistence expectations for continuation artifacts
    - Topology-prose discipline for this branch
    out:
    - Parent boundary and human-approval policy retained by SG-SPEC-0003
    - Concrete reflective execution mechanics owned by SG-SPEC-0027
    - Concrete candidate derivation, de-duplication, deterministic tie-break, and one-of-n selection owned
      by SG-SPEC-0033
    - Scoring formulas, queue ranking engines, and viewer UX
    - Concrete continuation artifact mutation contracts
  continuation_boundary:
  - SG-SPEC-0026 decides whether a completed reflective run may continue directly to another bounded target.
  - The decision is based on normalized candidate-selection output plus execution-readiness state, not
    on graph topology prose.
  decision_inputs:
    candidate_selection:
    - SG-SPEC-0033 owns deterministic candidate derivation, de-duplication, and selection ordering.
    execution_readiness:
    - SG-SPEC-0027 owns the reflective execution-readiness surface consumed by this decision.
  decision_contract:
  - Direct continuation is allowed only when candidate selection returns one deterministic result and
    execution readiness does not leave a blocking gate.
  - Continuation is suppressed when authority, existing proposals, active gates, or ambiguous selection
    make direct handoff unsafe.
  continuation_selection:
    preconditions:
    - The current bounded target has no remaining explicit blocked or gated work within current authority.
    - Continuation selection does not run as a bypass for an active operator_target override.
    - Continuation selection flow is delegated to SG-SPEC-0033 and bounded to one selected result if available.
    selection_flow:
    - SG-SPEC-0026 invokes SG-SPEC-0033 with normalized bounded context to retrieve at most one deterministic
      continuation selection result for handoff.
    - If SG-SPEC-0033 returns no valid deterministic selection (including tie ambiguity or missing ordering),
      SG-SPEC-0026 treats continuation as suppressed and emits no direct continuation mutation.
    suppression:
    - Do not select direct continuation when required authority exceeds the current continuation envelope.
    - Do not select direct continuation when an active proposal already owns the exact target region.
    - Do not select direct continuation when a gate remains active for the candidate concern.
    handoff:
    - Suppressed continuation becomes derived artifact emission or refresh rather than canonical mutation.
    - Continuation records must not set canonical backlog labels, ownership fields, status, or maturity
      directly.
  topology_prose_policy:
  - Canonical graph fields already represent which specs provide candidate selection and execution readiness.
  - SG-SPEC-0026 prose should explain the continuation decision semantics rather than restating child
    routing.
  boundaries:
  - SG-SPEC-0026 does not redefine parent governance boundaries.
  - Continuation records and delegated mechanics outputs remain non-canonical until separately authorized
    execution or approval paths apply them.
gate_state: none
proposed_status: null
proposed_maturity: null
required_human_action: '-'
last_gate_decision: approve
last_gate_note: ''
last_gate_at: '2026-04-14T08:03:14.530540+00:00'
