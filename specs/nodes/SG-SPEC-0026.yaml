id: SG-SPEC-0026
title: SpecGraph - Reflective Mechanics Integration and Continuation Selection
kind: spec
status: linked
maturity: 0.89
depends_on:
- SG-SPEC-0027
refines:
- SG-SPEC-0003
inputs:
- specs/nodes/SG-SPEC-0003.yaml
- specs/nodes/SG-SPEC-0004.yaml
- specs/nodes/SG-SPEC-0005.yaml
- specs/nodes/SG-SPEC-0006.yaml
- specs/nodes/SG-SPEC-0025.yaml
- tasks.md
outputs:
- specs/nodes/SG-SPEC-0026.yaml
allowed_paths:
- specs/nodes/SG-SPEC-0026.yaml
acceptance:
- Defines the bounded continuation-selection integration layer under SG-SPEC-0003 and delegates concrete
  reflective execution mechanics to a dedicated child cluster node.
- Defines the bounded continuation-selection policy for deriving, de-duplicating, and choosing at most
  one linked latent_graph_improvement_candidate after explicit blocked or gated work clears.
- Defines suppression and handoff behavior when continuation cannot proceed because authority limits,
  active proposal ownership, or active gates keep follow-up work non-executable.
- Remains implementation-agnostic about scoring formulas, queue prioritization engines, and viewer UX
  while keeping continuation and delegated mechanics non-canonical until explicit execution or approval.
acceptance_evidence:
- criterion: Defines the bounded continuation-selection integration layer under SG-SPEC-0003 and delegates
    concrete reflective execution mechanics to a dedicated child cluster node.
  evidence: specification.integration_role defines SG-SPEC-0026 as the continuation-selection integration
    child beneath SG-SPEC-0003, while specification.delegated_execution_cluster assigns SG-SPEC-0027 as
    the bounded mechanics cluster through which reflective execution descendants are consumed.
- criterion: Defines the bounded continuation-selection policy for deriving, de-duplicating, and choosing
    at most one linked latent_graph_improvement_candidate after explicit blocked or gated work clears.
  evidence: specification.continuation_selection.preconditions gates continuation to runs whose current
    bounded target has no remaining explicit blocked or gated work. specification.continuation_selection.candidate_derivation
    restricts derivation to linked canonical specs plus active graph-health signals, and specification.continuation_selection.selection
    limits the outcome to at most one next candidate with deterministic tie-breaking.
- criterion: Defines suppression and handoff behavior when continuation cannot proceed because authority
    limits, active proposal ownership, or active gates keep follow-up work non-executable.
  evidence: specification.continuation_selection.suppression blocks direct continuation when current authority
    is insufficient, when an active proposal already owns the exact target region, or when a gate is active
    for the candidate concern. specification.continuation_selection.handoff then requires derived continuation
    artifacts instead of canonical mutation.
- criterion: Remains implementation-agnostic about scoring formulas, queue prioritization engines, and
    viewer UX while keeping continuation and delegated mechanics non-canonical until explicit execution
    or approval.
  evidence: specification.scope.out excludes scoring formulas, queue prioritization engines, and viewer
    UX. specification.boundary_notes states that continuation records and delegated mechanics outputs
    remain derived or review-gated rather than canonical graph truth unless a separately authorized path
    applies them.
prompt: Keep SG-SPEC-0026 as the bounded integration child under SG-SPEC-0003 for reflective execution
  continuation. It should define deterministic continuation selection, delegate concrete reflective execution
  mechanics to SG-SPEC-0027, and specify suppression and handoff behavior without drifting into scoring
  formulas, proposal-lane precedence engines, or viewer design.
specification:
  objective: Define the bounded continuation-selection integration layer under SG-SPEC-0003 that owns
    continuation policy while delegating concrete reflective execution mechanics to SG-SPEC-0027.
  scope:
    in:
    - Integration role between SG-SPEC-0003 and delegated continuation or execution descendants
    - Continuation-selection preconditions, candidate derivation, and at-most-one selection behavior
    - Suppression and handoff behavior when continuation cannot proceed directly
    - Non-canonical persistence expectations for continuation artifacts
    out:
    - Parent boundary and human-approval policy retained by SG-SPEC-0003
    - Concrete reflective execution mechanics grouped under SG-SPEC-0027
    - Scoring formulas, queue ranking engines, and viewer UX
  integration_role:
  - SG-SPEC-0026 is the intermediate child that keeps SG-SPEC-0003 below the blocking-child atomicity
    limit while preserving explicit continuation-selection governance.
  - SG-SPEC-0026 may depend_on exactly one bounded execution cluster child when multiple reflective execution
    mechanics remain jointly required before continuation-aware operation is coherent.
  delegated_execution_cluster:
  - SG-SPEC-0027 owns the integrated reflective execution mechanics cluster beneath SG-SPEC-0026.
  - Execution-mechanics descendants that belong to this cluster are refined through SG-SPEC-0027.
  - SG-SPEC-0026 may depend directly only on SG-SPEC-0027 for execution-readiness; no other direct execution-mechanics
    dependencies are allowed while this cluster is present.
  continuation_selection:
    preconditions:
    - The current bounded target has no remaining explicit blocked or gated work within current authority.
    - Continuation selection does not run as a bypass for an active operator_target override.
    - Candidate discovery uses current canonical links, current status and maturity context, and active
      graph-health signals only.
    candidate_derivation:
    - Candidates are limited to already linked canonical specs in the current reflective frontier.
    - Low maturity may inform derivation only when combined with already-defined graph-health signals
      such as stalled_maturity_candidate, weak_structural_linkage_candidate, or repeated_split_required_candidate.
    - Candidate derivation must de-duplicate identical target regions within the same run context.
    selection:
    - At most one linked latent_graph_improvement_candidate may be selected per run.
    - 'Deterministic tie-breaking is strictly ordered by signal class: first repeated_split_required_candidate,
      then stalled_maturity_candidate, then weak_structural_linkage_candidate; if still tied, choose the
      lexicographically smallest linked spec identifier and defer all other candidates to derived handoff.'
    - If no eligible candidate survives derivation and suppression, no continuation selection occurs.
    suppression:
    - Do not select direct continuation when required authority exceeds the current continuation envelope.
    - Do not select direct continuation when an active proposal already owns the exact target region.
    - Do not select direct continuation when a gate remains active for the candidate concern.
    handoff:
    - Suppressed continuation becomes derived artifact emission or refresh rather than canonical mutation.
    - Continuation records must not set canonical backlog labels, ownership fields, status, or maturity
      directly.
  boundary_notes:
  - SG-SPEC-0026 integrates continuation rules and delegated execution clustering but does not redefine
    parent governance boundaries.
  - Continuation records and delegated mechanics outputs remain non-canonical until separately authorized
    execution or approval paths apply them.
gate_state: split_required
required_human_action: split spec scope before rerun
proposed_status: null
proposed_maturity: null
last_outcome: split_required
last_blocker: ''
last_run_id: 20260414T063826Z-SG-SPEC-0026
last_exit_code: 0
last_changed_files:
- specs/nodes/SG-SPEC-0026.yaml
last_run_at: '2026-04-14T06:38:27.249213+00:00'
last_worktree_path: /Users/egor/Development/GitHub/0AL/SpecGraph/.worktrees/sg-spec-0026-20260414T063805Z
last_branch: codex/sg-spec-0026/20260414T063805Z
last_validator_results:
  outputs: true
  allowed_paths: true
  reconciliation: true
  atomicity: true
  transition: true
  executor_environment: true
  refinement_acceptance: true
last_refinement_acceptance:
  decision: approve
  change_class: local_refinement
  checks:
    content_changed: true
    hard_validation: true
    single_spec_scope: true
    constitutional_diff: false
    graph_refactor_diff: false
    atomicity_clear: true
    within_mutation_budget: true
  diff_paths:
  - acceptance_evidence[0].evidence
  - specification.delegated_execution_cluster[1]
  - specification.delegated_execution_cluster[2]
  mutation_classes:
  - policy_text
  mutation_budget: []
  budget_exceeded_classes: []
  changed_spec_files:
  - specs/nodes/SG-SPEC-0026.yaml
  review_reasons: []
  errors: []
  warnings: []
last_reconciliation:
  worktree_spec_count: 32
  changed_spec_ids:
  - SG-SPEC-0026
  semantic_dependencies_resolved: true
  work_dependencies_ready: false
  cycles: []
last_requested_child_materialization: false
last_materialized_child_paths: []
