id: SG-SPEC-0050
title: SpecGraph - Pre-Spec Semantic Layer for Observable Intent
kind: spec
status: specified
maturity: 0.35
depends_on:
- SG-SPEC-0051
- SG-SPEC-0052
relates_to:
- SG-SPEC-0006
- SG-SPEC-0009
refines:
- SG-SPEC-0007
inputs:
- specs/nodes/SG-SPEC-0007.yaml
- specs/nodes/SG-SPEC-0051.yaml
- specs/nodes/SG-SPEC-0052.yaml
- specs/nodes/SG-SPEC-0009.yaml
- specs/nodes/SG-SPEC-0006.yaml
- docs/proposals/0002_intent_layer.md
- docs/proposals/0005_intent_to_proposal_workflow.md
- docs/proposals/0011_pre_spec_semantic_layer.md
outputs:
- specs/nodes/SG-SPEC-0050.yaml
allowed_paths:
- specs/nodes/SG-SPEC-0050.yaml
acceptance:
- Defines the primary pre-spec semantic kinds Intent, Hypothesis, and Proposal as reviewable pre-canonical
  declarations rather than canonical graph truth
- Defines IntentFragment and IntentDraft as the minimal observed-intent path between raw user input and
  later Proposal or SpecDraft material
- Declares orthogonal pre-spec axes for phase, status, authority, confidence, provenance, context_tags,
  and supersedes, and delegates their concrete value spaces plus traceability semantics to child SG-SPEC-0052
  so assistant-generated draft declarations remain inspectable and non-magical
- Delegates the concrete promotion boundary between IntentDraft, Proposal, SpecDraft, and canonical materialization
  to child SG-SPEC-0051, including explicit Proposal->SpecDraft review and non-silent promotion constraints
- Keeps memory consultation policy, operator-request execution steering, tracked proposal-lane persistence,
  GUI protocol, and final storage layout out of scope for follow-on nodes
acceptance_evidence:
- criterion: Defines the primary pre-spec semantic kinds Intent, Hypothesis, and Proposal as reviewable
    pre-canonical declarations rather than canonical graph truth
  evidence: specification.terminology defines intent, hypothesis, and proposal, while specification.boundary_policy.pre_canonical_status
    states that these kinds belong to the pre-spec layer and remain reviewable draft declarations until
    an explicit promotion step materializes a governed canonical artifact.
- criterion: Defines IntentFragment and IntentDraft as the minimal observed-intent path between raw user
    input and later Proposal or SpecDraft material
  evidence: specification.terminology defines intent_fragment and intent_draft, and specification.observation_model
    defines the path raw_input -> IntentFragment -> IntentDraft together with the rule that one draft
    may synthesize multiple fragments plus linked constraints, questions, or hypotheses.
- criterion: Declares orthogonal pre-spec axes for phase, status, authority, confidence, provenance, context_tags,
    and supersedes, and delegates their concrete value spaces plus traceability semantics to child SG-SPEC-0052
    so assistant-generated draft declarations remain inspectable and non-magical
  evidence: specification.node_axes.retained_parent_role keeps phase, status, authority, confidence, provenance,
    context_tags, and supersedes as explicit review axes adjacent to kind, specification.node_axes.delegated_to
    assigns concrete value spaces, defaulting rules, and traceability semantics to SG-SPEC-0052, and specification.boundary_policy.assistant_visibility
    requires assistant-generated nodes to preserve those axes so review can distinguish inferred draft
    material from user-originated or promoted artifacts.
- criterion: Delegates the concrete promotion boundary between IntentDraft, Proposal, SpecDraft, and canonical
    materialization to child SG-SPEC-0051, including explicit Proposal->SpecDraft review and non-silent
    promotion constraints
  evidence: specification.promotion_pipeline.delegated_to names `SG-SPEC-0051`, specification.promotion_pipeline.delegation_note
    says that concrete pre-spec promotion gates and explicit Proposal->SpecDraft review live in the child,
    and specification.adjacency_notes explicitly delegate those non-silent promotion constraints away
    from this parent overview node.
- criterion: Keeps memory consultation policy, operator-request execution steering, tracked proposal-lane
    persistence, GUI protocol, and final storage layout out of scope for follow-on nodes
  evidence: specification.scope.out excludes project-memory consultation policy, supervisor-facing operator-request
    execution semantics, tracked proposal-lane storage and authority, GUI protocol, and repository layout.
    specification.adjacency_notes names SG-SPEC-0008, SG-SPEC-0009, and SG-SPEC-0006 as adjacent follow-on
    concerns rather than concerns resolved here.
prompt: Define the first canonical child under SG-SPEC-0007 for the pre-spec semantic layer. Keep it focused
  on Intent, Hypothesis, Proposal, IntentFragment, IntentDraft, orthogonal provenance or authority axes,
  and promotion boundaries into Proposal or SpecDraft. Delegate explicit Proposal->SpecDraft review and
  non-silent transition constraints to child SG-SPEC-0051, and delegate concrete orthogonal axis value
  spaces plus traceability semantics to child SG-SPEC-0052. Do not define project-memory consultation
  policy, operator-request runtime execution semantics, tracked proposal-lane storage, GUI protocol, or
  repository layout here.
specification:
  objective: Define the minimal graph-native semantic layer that captures observable pre-spec intent formation
    before canonical spec or proposal promotion.
  scope:
    in:
    - Primary pre-spec semantic kinds Intent, Hypothesis, and Proposal
    - Auxiliary observed-intent kinds IntentFragment and IntentDraft
    - Orthogonal draft axes such as phase, status, authority, confidence, provenance, context_tags, and
      supersedes
    - Suggested edge semantics between pre-spec declarations
    out:
    - Project-memory consultation policy or retrieval behavior
    - Supervisor-facing operator-request execution contract
    - Tracked proposal-lane repository persistence and authority states
    - GUI protocol, canvas layout, or renderer behavior
    - Final repository storage layout for pre-spec artifacts
    - Full natural-language understanding or model-specific interpretation logic
  terminology:
    pre_spec_layer: Graph-native, reviewable semantics that capture intent formation before canonical
      spec or proposal artifacts are accepted as governed graph truth.
    raw_input: A human-originated utterance, note, or selection-adjacent expression that has not yet been
      normalized into pre-spec graph structure.
    intent: A normative declaration of what should become true, remain possible, be preserved, or be prevented.
    hypothesis: An epistemic declaration of what may be true in the problem space.
    proposal: A candidate design move intended to operationalize one or more intents under constraints
      and assumptions. A Proposal is pre-canonical and remains non-authoritative until explicitly linked
      to a Review or promotion decision.
    intent_fragment: The minimal extracted semantic unit from raw_input that appears to express part of
      an intent but is not yet stable enough to become a full draft.
    intent_draft: A synthesized pre-spec node assembled from one or more intent_fragment nodes plus optional
      linked constraints, questions, and hypotheses.
    spec_draft: A reviewable pre-canonical draft of later specification shape that may be promoted only
      from confirmed or explicitly reviewed pre-spec material.
  node_axes:
    delegated_to: SG-SPEC-0052
    retained_parent_role:
    - phase, status, authority, confidence, provenance, context_tags, and supersedes remain explicit review
      axes adjacent to pre-spec kind, not separate primary semantic kinds.
    - The parent only establishes that these axes exist for pre-spec nodes and must remain inspectable
      through review.
    - Concrete value spaces, defaulting rules, traceability requirements, and supersession immutability
      semantics are delegated to SG-SPEC-0052.
  observation_model:
    fragment_rules:
    - intent_fragment is the smallest extracted semantic unit that still carries observable intent meaning.
    - Fragments may preserve partial goals, objections, constraints, or questions without forcing one
      final interpretation too early.
    draft_rules:
    - intent_draft may synthesize one or more intent_fragment nodes plus linked constraints, questions,
      and hypotheses.
    - intent_draft remains the primary observed-intent object until explicit confirmation, review, or
      promotion occurs.
    - A single raw_input may produce multiple drafts when one utterance carries multiple bounded concerns.
  edge_semantics:
  - intent_fragment refines intent_draft
  - constraint constrains intent_draft or proposal
  - proposal operationalizes intent
  - proposal assumes hypothesis
  - question answers or blocks proposal
  - intent_draft derived_from raw_input or precursor graph context
  promotion_pipeline:
    delegated_to: SG-SPEC-0051
    delegation_note: SG-SPEC-0050 defines pre-spec semantic kinds and aggregate adjacency; SG-SPEC-0051
      governs the concrete pre-spec promotion gates, non-silent transition requirements, and explicit
      Proposal->SpecDraft review before canonical materialization, while SG-SPEC-0052 governs concrete
      orthogonal axis value spaces and traceability semantics.
  boundary_policy:
    pre_canonical_status:
    - Pre-spec nodes are reviewable declarations, not canonical graph truth.
    - Presence in the pre-spec layer does not by itself mutate canonical intent or specification nodes.
    assistant_visibility:
    - Assistant-generated pre-spec nodes must preserve authority, confidence, and provenance so their
      synthesis remains inspectable.
    context_boundary:
    - Context should default to metadata, overlays, selectors, filters, or weighting lenses rather than
      to a separate primary semantic kind.
  adjacency_notes:
  - SG-SPEC-0051 defines the concrete promotion boundary for IntentDraft, Proposal, SpecDraft, and canonical
    materialization, including explicit Proposal->SpecDraft review and non-silent transition constraints.
  - SG-SPEC-0052 defines the orthogonal axis value spaces and traceability semantics for pre-spec nodes
    using phase, status, authority, confidence, provenance, context_tags, and supersedes.
  - SG-SPEC-0008 remains the adjacent node for explicit project-memory consultation during mediated discovery.
  - SG-SPEC-0009 remains the adjacent node for one-run operator-request normalization and supervisor steering.
  - SG-SPEC-0006 remains the adjacent node for tracked proposal-lane persistence, review state, and application
    semantics.
  - This node defines semantic kinds and promotion boundaries, not final repository storage layout or
    viewer rendering.
  examples:
  - name: Intent-first draft
    description: A user says the original intent should survive formalization. The system captures fragments,
      synthesizes an intent_draft, and keeps it reviewable before any canonical spec node is created.
  - name: Hypothesis-backed proposal
    description: The system records a hypothesis that premature proposals bias framing, then links a proposal
      to that hypothesis and to the motivating intent rather than silently flattening both into one spec.
gate_state: split_required
required_human_action: split spec scope before rerun
proposed_status: null
proposed_maturity: null
last_outcome: split_required
last_blocker: ''
last_run_id: 20260415T221011Z-SG-SPEC-0050
last_exit_code: 0
last_changed_files:
- specs/nodes/SG-SPEC-0050.yaml
- specs/nodes/SG-SPEC-0052.yaml
last_run_at: '2026-04-15T22:10:12.426970+00:00'
last_worktree_path: ''
last_branch: ''
last_validator_results:
  outputs: true
  allowed_paths: true
  reconciliation: true
  atomicity: true
  transition: true
  executor_environment: true
  refinement_acceptance: true
last_refinement_acceptance:
  decision: review_required
  change_class: graph_refactor
  checks:
    content_changed: true
    hard_validation: true
    single_spec_scope: false
    constitutional_diff: false
    graph_refactor_diff: true
    atomicity_clear: true
    within_mutation_budget: true
  diff_paths:
  - specification.adjacency_notes[1]
  - specification.adjacency_notes[2]
  - specification.adjacency_notes[3]
  - specification.adjacency_notes[4]
  - specification.adjacency_notes[5]
  mutation_classes:
  - policy_text
  mutation_budget: []
  budget_exceeded_classes: []
  changed_spec_files:
  - specs/nodes/SG-SPEC-0050.yaml
  - specs/nodes/SG-SPEC-0052.yaml
  review_reasons:
  - run changed additional spec files beyond the selected source node
  errors: []
  warnings: []
last_reconciliation:
  worktree_spec_count: 52
  changed_spec_ids:
  - SG-SPEC-0050
  - SG-SPEC-0052
  semantic_dependencies_resolved: true
  work_dependencies_ready: false
  cycles: []
last_requested_child_materialization: true
last_materialized_child_paths:
- specs/nodes/SG-SPEC-0052.yaml
