id: SG-SPEC-0056
title: SpecGraph - Historical Non-Retry Consumer-Chain Boundary
kind: spec
created_at: '2026-04-24T21:06:00Z'
updated_at: '2026-04-25T18:31:32Z'
status: reviewed
maturity: 0.23
presence:
  state: historical
  lineage_basis: superseded_lineage
depends_on:
- SG-SPEC-0040
refines:
- SG-SPEC-0040
superseded_by:
- SG-SPEC-0040
inputs:
- specs/nodes/SG-SPEC-0040.yaml
- tasks.md
outputs:
- specs/nodes/SG-SPEC-0056.yaml
allowed_paths:
- specs/nodes/SG-SPEC-0056.yaml
acceptance:
- Records SG-SPEC-0056 as a superseded consumer-chain split artifact whose active role is now carried
  by SG-SPEC-0040.
- Preserves inspectable lineage for the earlier SG-SPEC-0039 consumer-chain split without keeping SG-SPEC-0056
  in active readiness topology.
- Keeps concrete non-retry queue-control consumption semantics active through SG-SPEC-0040 rather than
  through this historical child.
acceptance_evidence:
- criterion: Records SG-SPEC-0056 as a superseded consumer-chain split artifact whose active role is now
    carried by SG-SPEC-0040.
  evidence: top-level presence marks SG-SPEC-0056 historical, superseded_by points to SG-SPEC-0040, and
    specification.historical_status states that SG-SPEC-0040 is the active first-persistence and noncanonical-consumption
    boundary.
- criterion: Preserves inspectable lineage for the earlier SG-SPEC-0039 consumer-chain split without keeping
    SG-SPEC-0056 in active readiness topology.
  evidence: specification.lineage_contract preserves the supersession relation to SG-SPEC-0040, while
    specification.inactive_rules prevents SG-SPEC-0056 from satisfying active readiness or direct consumer-chain
    dependency checks.
- criterion: Keeps concrete non-retry queue-control consumption semantics active through SG-SPEC-0040
    rather than through this historical child.
  evidence: specification.active_successor_boundary names SG-SPEC-0040 as the active owner of first-persistence,
    noncanonical-consumption, and consumer-rule semantics for dedupe_key, preempt_direct_update, and proposal_direct_handoff.
prompt: Preserve SG-SPEC-0056 only as inspectable lineage for a superseded consumer-chain split beneath
  SG-SPEC-0039. Active first-persistence and noncanonical-consumption semantics now live in SG-SPEC-0040.
specification:
  objective: Preserve SG-SPEC-0056 as historical lineage for a superseded consumer-chain split while keeping
    the active non-retry queue-control consumption boundary in SG-SPEC-0040.
  scope:
    in:
    - Historical lineage for the earlier SG-SPEC-0039 consumer-chain split.
    - Explicit superseded_by relation to SG-SPEC-0040.
    - Inactive-child rules for active readiness and graph-health inspection.
    out:
    - Active consumer-chain semantics for non-retry queue-control metadata.
    - Concrete non-retry routing semantics, artifact-shape contracts, retry-condition behavior, or authority
      transitions.
    - Any new child materialization or decomposition authority.
  historical_status:
  - SG-SPEC-0056 records a prior attempt to isolate non-retry consumer-chain constraints as a separate
    child beneath SG-SPEC-0039.
  - SG-SPEC-0056 is superseded by SG-SPEC-0040 and remains inspectable lineage only.
  lineage_contract:
  - SG-SPEC-0040 supersedes SG-SPEC-0056 as the active first-persistence and noncanonical-consumption
    boundary for this cluster.
  - SG-SPEC-0056 refines SG-SPEC-0040 only as historical lineage, not as an active child for continued
    splitting.
  active_successor_boundary:
  - SG-SPEC-0040 owns active first-persistence and noncanonical-consumption semantics for dedupe_key,
    preempt_direct_update, and proposal_direct_handoff.
  - SG-SPEC-0039 consumes SG-SPEC-0040 as the active child for the noncanonical consumption boundary.
  inactive_rules:
  - Graph-health inspection must exclude SG-SPEC-0056 from the active SG-SPEC-0039 subtree while superseded_by
    points to SG-SPEC-0040.
  - Future refinement should target SG-SPEC-0040 for this concern unless a later reviewed supersession
    changes lineage again.
  boundaries:
  - SG-SPEC-0056 no longer defines an active node_role, composed_function, or downstream contract.
  - This node exists only to preserve inspectable lineage for the superseded consumer-chain split.
